var stream = require('stream');
var util = require('util');

exports.initialize = function(agent) {

/*
    function MonisAgentStream() {
        console.log("New stream " + this);
        
        var emitFunction = this.emit;
        this.emit = function(type) {
            console.log("Emit: " + type);
            emitFunction(type);
        };
    };
    
    util.inherits(MonisAgentStream, stream.Stream);
    
    stream.Stream = MonisAgentStream;
    */
};